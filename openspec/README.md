# Frozen OpenSpec history

**Status:** frozen historical reference since 2026-07-16

This directory preserves the repository's former OpenSpec capability specs, change archives, configuration, and integrity metadata so existing links and design history remain traceable. It is not an active engineering workflow and does not define current behavior.

Current behavior is established by the code, executable tests, and documents marked `Current` in `docs/index.md`. Verify any information found here against those maintained sources before relying on it.

Do not add, edit, synchronize, or archive OpenSpec artifacts unless the maintainer explicitly requests a correction to the historical record. New engineering work follows the Matt skills policy in `docs/agents/workflow.md` and is retained under `.scratch/`.
