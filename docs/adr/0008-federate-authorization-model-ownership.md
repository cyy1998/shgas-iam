---
status: accepted
---

# 联邦拥有业务授权模型并由 IAM 集中治理

每个 client 所属业务域拥有自己的 Client Authorization Contract，包括命名空间、资源类型、动作和资源属性语义，并在请求时提供 IAM 无法推断的实时资源事实；IAM 拥有统一决策信封、共享主体属性、公共上下文、安全基线以及策略的审批、签名、发布和审计。相比由 IAM 团队集中定义所有业务资源，该划分使模型与资源生命周期保持同一所有者；相比各应用完全自治，它仍以集中发布和全局基线维持跨系统的一致治理。
