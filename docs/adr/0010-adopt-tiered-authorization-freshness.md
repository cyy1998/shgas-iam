---
status: accepted
---

# 对授权事实采用分级新鲜度契约

分布式授权判定不承诺全局零延迟一致：账号禁用、离职和紧急封禁等紧急撤权必须在 30 秒内被所有相关 Authorization Enforcement Boundary 观察，且边界无法证明紧急状态足够新鲜时对敏感操作拒绝放行；普通角色、主体属性和已发布策略变化必须在 2 分钟内可见；资源状态和资源属性由资源所有者在请求时提供当前事实。该分级在避免中央逐请求判定的同时，为撤权风险设定有界窗口，并要求实现通过分发状态和 freshness deadline 证明而非假设满足时限。
