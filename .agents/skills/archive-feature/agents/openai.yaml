interface:
  display_name: "Archive Feature"
  short_description: "Safely archive a completed feature tracker"
  default_prompt: "Use $archive-feature to archive the completed tracker for the current feature branch."

policy:
  allow_implicit_invocation: true
