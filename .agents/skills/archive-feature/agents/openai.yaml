interface:
  display_name: "Archive Feature"
  short_description: "Safely archive a completed feature tracker"
  default_prompt: "Use $archive-feature only when the user explicitly asks to archive a feature or approves an immediately preceding closeout prompt that names the feature and its archive step."

policy:
  allow_implicit_invocation: true
