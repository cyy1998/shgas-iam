{
  "name": "iam-monorepo",
  "packageManager": "pnpm@11.1.2",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "gateway:apisix": "bun gateway/apisix/scripts/apisix-sync.ts",
    "gateway:apisix:validate": "bun gateway/apisix/scripts/apisix-sync.ts validate",
    "gateway:apisix:diff": "bun gateway/apisix/scripts/apisix-sync.ts diff",
    "gateway:apisix:apply": "bun gateway/apisix/scripts/apisix-sync.ts apply"
  },
  "devDependencies": {
    "turbo": "^2.9.14",
    "yaml": "^2.8.2"
  },
  "pnpm": {
    "overrides": {
      "typescript": "6.0.3"
    }
  }
}
