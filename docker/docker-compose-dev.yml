x-build-proxy-args: &build-proxy-args
  HTTP_PROXY: ${DOCKER_BUILD_HTTP_PROXY:-}
  HTTPS_PROXY: ${DOCKER_BUILD_HTTPS_PROXY:-}
  NO_PROXY: ${DOCKER_BUILD_NO_PROXY:-}
  http_proxy: ${DOCKER_BUILD_HTTP_PROXY:-}
  https_proxy: ${DOCKER_BUILD_HTTPS_PROXY:-}
  no_proxy: ${DOCKER_BUILD_NO_PROXY:-}

x-api-env: &api-env
  DATABASE_URL: ${API_DATABASE_URL:-postgresql://${POSTGRES_USER:-iam}:${POSTGRES_PASSWORD:-iam_password}@db:5432/${POSTGRES_DB:-iam_db}}
  REDIS_URL: ${API_REDIS_URL:-redis}
  REDIS_PORT: ${API_REDIS_PORT:-6379}
  REDIS_PASSWORD: ${API_REDIS_PASSWORD:-}
  REDIS_DB: ${API_REDIS_DB:-0}
  PORT: ${API_PORT:-30000}
  NODE_ENV: ${API_NODE_ENV:-production}
  LOG_LEVEL: ${API_LOG_LEVEL:-info}
  LOG_FORMAT: ${API_LOG_FORMAT:-json}
  IAM_SECRET_KEY: ${IAM_SECRET_KEY:-dev-iam-secret-change-me}
  PASSWORD_HASH_ROUNDS: ${PASSWORD_HASH_ROUNDS:-10}
  DEFAULT_USER_PASSWORD: ${DEFAULT_USER_PASSWORD:-default123}
  MAGIC_CODE: ${MAGIC_CODE:-1234}
  CAP_ENABLED: ${CAP_ENABLED:-true}
  CAP_SITE_KEY: ${CAP_SITE_KEY:-iam-sso}
  CAP_SECRET: ${CAP_SECRET:-dev-cap-secret-change-me}
  CAP_CHALLENGE_TTL_MS: ${CAP_CHALLENGE_TTL_MS:-600000}
  CAP_TOKEN_TTL_SECONDS: ${CAP_TOKEN_TTL_SECONDS:-600}
  HUMAN_VERIFICATION_WINDOW_SECONDS: ${HUMAN_VERIFICATION_WINDOW_SECONDS:-600}
  HUMAN_VERIFICATION_LOGIN_FAILURE_THRESHOLD: ${HUMAN_VERIFICATION_LOGIN_FAILURE_THRESHOLD:-3}
  HUMAN_VERIFICATION_LOOKUP_THRESHOLD: ${HUMAN_VERIFICATION_LOOKUP_THRESHOLD:-20}
  LOGIN_CREDENTIAL_ACTIVE_KID: ${LOGIN_CREDENTIAL_ACTIVE_KID:-2026-05-primary}
  LOGIN_CREDENTIAL_PRIVATE_KEYS_JSON: ${LOGIN_CREDENTIAL_PRIVATE_KEYS_JSON:-{"2026-05-primary":"319b4e59ca80d7b4cc35955b63da4edf1ed51772ec8f33c0a4f769dda7b9fc65"}}
  LOGIN_CREDENTIAL_MAX_SKEW_MS: ${LOGIN_CREDENTIAL_MAX_SKEW_MS:-300000}
  LOGIN_CREDENTIAL_NONCE_TTL_SECONDS: ${LOGIN_CREDENTIAL_NONCE_TTL_SECONDS:-360}
  WX_CORPID: ${WX_CORPID:-dev-wechat-corp-id}
  WX_CORPSECRET: ${WX_CORPSECRET:-dev-wechat-corp-secret}
  SMS_URL: ${SMS_URL:-http://sms-service.localhost/api/send}
  SMS_SIGNATURE_KEY: ${SMS_SIGNATURE_KEY:-dev-sms-signature-key}
  ORCAS_URL: ${ORCAS_URL:-http://orcas-service.localhost/orcas/login}
  PURVEYOR_PARENT_ORG: ${PURVEYOR_PARENT_ORG:-GY}
  REDIS_EXPIRE_TIME: ${REDIS_EXPIRE_TIME:-86400}
  AUTH_CODE_EXPIRE_TIME: ${AUTH_CODE_EXPIRE_TIME:-300}
  LOGIN_ENDPOINT: ${LOGIN_ENDPOINT:-/portal/login}
  SSO_INTERNAL_ORIGIN: ${SSO_INTERNAL_ORIGIN:-http://localhost:30080}
  SSO_EXTERNAL_ORIGIN: ${SSO_EXTERNAL_ORIGIN:-http://localhost:39999}
  AUTHORIZATION_ENDPOINT: ${AUTHORIZATION_ENDPOINT:-/sso/authorize}
  LOGOUT_ENDPOINT: ${LOGOUT_ENDPOINT:-/sso/logout}
  THIRDPARTY_OA_ENDPOINT: ${THIRDPARTY_OA_ENDPOINT:-/sso/thirdparty/oa}
  HTTP_PROXY: ${API_HTTP_PROXY:-}
  HTTPS_PROXY: ${API_HTTPS_PROXY:-}
  NO_PROXY: ${API_NO_PROXY:-localhost,127.0.0.1,db,redis,apisix,apisix-etcd}

x-admin-api-env: &admin-api-env
  DATABASE_URL: ${ADMIN_API_DATABASE_URL:-postgresql://${POSTGRES_USER:-iam}:${POSTGRES_PASSWORD:-iam_password}@db:5432/${POSTGRES_DB:-iam_db}}
  REDIS_URL: ${ADMIN_API_REDIS_URL:-redis}
  REDIS_PORT: ${ADMIN_API_REDIS_PORT:-6379}
  REDIS_PASSWORD: ${ADMIN_API_REDIS_PASSWORD:-}
  REDIS_DB: ${ADMIN_API_REDIS_DB:-0}
  PORT: ${ADMIN_API_PORT:-30001}
  NODE_ENV: ${ADMIN_API_NODE_ENV:-production}
  LOG_LEVEL: ${ADMIN_API_LOG_LEVEL:-info}
  LOG_FORMAT: ${ADMIN_API_LOG_FORMAT:-json}
  PASSWORD_HASH_ROUNDS: ${PASSWORD_HASH_ROUNDS:-10}
  ADMIN_CLIENT_CODES: ${ADMIN_CLIENT_CODES:-iam-admin}
  ADMIN_ROLE_CODES: ${ADMIN_ROLE_CODES:-iam:admin}

x-oidc-provider-env: &oidc-provider-env
  DATABASE_URL: ${OIDC_DATABASE_URL:-postgresql://${POSTGRES_USER:-iam}:${POSTGRES_PASSWORD:-iam_password}@db:5432/${POSTGRES_DB:-iam_db}}
  REDIS_URL: ${OIDC_REDIS_URL:-redis}
  REDIS_PORT: ${OIDC_REDIS_PORT:-6379}
  REDIS_PASSWORD: ${OIDC_REDIS_PASSWORD:-}
  REDIS_DB: ${OIDC_REDIS_DB:-0}
  PORT: ${OIDC_PORT:-30002}
  NODE_ENV: ${OIDC_NODE_ENV:-development}
  LOG_LEVEL: ${OIDC_LOG_LEVEL:-info}
  LOG_FORMAT: ${OIDC_LOG_FORMAT:-json}
  OIDC_ISSUER: ${OIDC_ISSUER:-http://localhost:30080/oidc}
  OIDC_PUBLIC_ORIGIN: ${OIDC_PUBLIC_ORIGIN:-http://localhost:30080}
  OIDC_SSO_LOGIN_PATH: ${OIDC_SSO_LOGIN_PATH:-/portal/login}
  OIDC_COOKIE_KEYS: ${OIDC_COOKIE_KEYS:-dev-cookie-key-primary-32-chars-change,dev-cookie-key-secondary-32-chars-change}
  OIDC_CURRENT_JWK_JSON: ${OIDC_CURRENT_JWK_JSON:-{"kty":"RSA","n":"nPB-gxsDDUQPxGJnIQAQL_snej2-I3IseZ7Zrnv5EtpquR8KSs_zgSBZ3wNCmxuG6gURwqdTZQAlPK8C-vD1IJhUsFtVQwwhotDlYAkNSReMdCA6YGBW-n5I0TA0_THEcA0lusVdBFzqzULZMe_7GXMy8vMSo4Tyx_N1qK8XG6BEa943oveXRqFw0K-YCcTbELFjUz6pHHrG9wRIGBo6YvmtFtGT4wo9JYH6Q-ag43o7ag5qnYoRCuG-CYTvpvucFHHQne1YbzJcdzdDYtXwFjGqAGgzHXwoiJh7VJ7I2SN8lvkijexIzNmE0Yo-e2GQoMiRmV2-1amvXRkS9e7aqw","e":"AQAB","d":"FZ1b10O897KZjKh-4AEpuZSuOxGjjS84PTyUajR3OVJYDjdeYVpjBQmcccYkV3ItnS_l4njNgrodPCPggF5NFy2pGwGhQb2QfmOl9m7uDlSOIB-g2qLk0oAvwuaQTb-bzfue6CqEOp8aKaYHfZoPQSc9a6TwZWa1_LWlIqk8FgOVWAClgcnt_xTwJnj_4KIXGz-DAI2yUe43nc5FsrbiktfHhYPcDvBz1P7ECWPQ1qoO5PlanZCO5IXCZiYW6tZhnA7dgmEzu9TeL9HfJZF5MHtxWyHVHpJfoRikBkfktmshIV4wuJ2xj1DuMsm_eTwR3LJHsk69IFWkukGeu-IE6Q","p":"0EVwhkmZOavvv5WfwVb5PcpK_6qserr5F8epagNqY2uTMsdEnRBaMAKWmR9RxFM0edg85ekd1OxClmhn27P4bhkM_dDPPf_U_K15Y3MuqQJ_9jzKHwiVAwzReRfC_fZpMhJV7R_9Skrros5s_rbQmE6AASaz4SwdIybn6lIgMGM","q":"wOeXwNnLIWfXykDUTAOV3JHBIFlSbocBOhF4JEW6JCDQHt0E1EsTFqE0AQ_wKqAyKTtThIlzD9SAtxUHgvD1CIDcf-k_JrXHqjgAgeiOBzYzw_FIzkRA4fzRFoc53CY1xxzFhjDh0KaRhsBT-b9QOLlT9Zf9X_gn82GZgesoqxk","dp":"Pei2RySviGYkkSWP5G_PDOAKJS99r8nOa5QtNtcnE8CRc-Gp8qo-tNDAJWoa6yn0ileN2qXN-VH1hTIp1TQNH-UQaI5eXEak8Cs8zyQY9aJJmU9-oXTynoJ77evzSXyqzniG1ELTDvdwTefgnGKJZQzIng78H232h1F0VMHu0ZM","dq":"A_e_g1P3faRpJ9vVggrA1lyZIFU2ANf-Cne5hOV6Qqv6G-hn5QX2SozeSPdO9VF-NqLlwOAi4bx5OUrRcLwMDjS4VFkz11P9215zhDiIU1WmEu2vla6JtxxAILp_UivnB0VsOqpeuq_yZlcmYwiwWbpWI8MQjxzHlZX4VlfD6mE","qi":"ljBnwPNVSHbFeStiXAlwO55Q7DEk3ci4gXbXry6YE3ztAXJMAC0WcMky9dzNx1dRU810KOMjfIMCGc_8D5bAownJfhK6SkKty_lBXx2wDseOrs0EnPQqF4vNfsJcCycMx4Th1ojAgfHvMyD0tyPIRI27EPZ5rfgeK-J7h0PBrMw","kid":"dev-example-rs256","alg":"RS256","use":"sig"}}
  OIDC_PREVIOUS_JWK_JSON: ${OIDC_PREVIOUS_JWK_JSON:-}
  OIDC_GLOBAL_SESSION_COOKIE: ${OIDC_GLOBAL_SESSION_COOKIE:-global_session}
  OIDC_GLOBAL_SESSION_TTL_SECONDS: ${OIDC_GLOBAL_SESSION_TTL_SECONDS:-86400}
  OIDC_AUTHORIZATION_CODE_TTL_SECONDS: ${OIDC_AUTHORIZATION_CODE_TTL_SECONDS:-300}
  OIDC_INTERACTION_TTL_SECONDS: ${OIDC_INTERACTION_TTL_SECONDS:-600}
  OIDC_ACCESS_TOKEN_TTL_SECONDS: ${OIDC_ACCESS_TOKEN_TTL_SECONDS:-3600}
  OIDC_ID_TOKEN_TTL_SECONDS: ${OIDC_ID_TOKEN_TTL_SECONDS:-3600}
  OIDC_CLIENT_CACHE_TTL_SECONDS: ${OIDC_CLIENT_CACHE_TTL_SECONDS:-60}
  OIDC_BCRYPT_COST: ${OIDC_BCRYPT_COST:-12}
  OIDC_CLIENT_AUTH_FAILURE_LIMIT: ${OIDC_CLIENT_AUTH_FAILURE_LIMIT:-5}
  OIDC_CLIENT_AUTH_FAILURE_WINDOW_SECONDS: ${OIDC_CLIENT_AUTH_FAILURE_WINDOW_SECONDS:-60}
  OIDC_TRUST_PROXY: ${OIDC_TRUST_PROXY:-true}

services:
  db:
    image: ${POSTGRES_IMAGE:-docker.xuanyuan.run/postgres:18}
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-iam}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-iam_password}
      POSTGRES_DB: ${POSTGRES_DB:-iam_db}
    ports:
      - "${POSTGRES_PUBLISHED_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-iam} -d ${POSTGRES_DB:-iam_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: ${REDIS_IMAGE:-docker.xuanyuan.run/library/redis:latest}
    restart: unless-stopped
    ports:
      - "${REDIS_PUBLISHED_PORT:-6390}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  apisix-etcd:
    image: ${APISIX_ETCD_IMAGE:-docker.xuanyuan.run/bitnamilegacy/etcd:3.5.11}
    restart: unless-stopped
    environment:
      ALLOW_NONE_AUTHENTICATION: "yes"
      ETCD_ADVERTISE_CLIENT_URLS: http://apisix-etcd:2379
      ETCD_LISTEN_CLIENT_URLS: http://0.0.0.0:2379
    volumes:
      - apisix_etcd_data:/bitnami/etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health", "--endpoints=http://127.0.0.1:2379"]
      interval: 10s
      timeout: 5s
      retries: 5

  apisix:
    image: ${APISIX_IMAGE:-docker.xuanyuan.run/apache/apisix:3.16.0-debian}
    restart: unless-stopped
    labels:
      shgas-iam.logs.enabled: "true"
      shgas-iam.logs.env: dev
      shgas-iam.logs.service: apisix
      shgas-iam.logs.component: gateway
    ports:
      - "${APISIX_HTTP_PUBLISHED_PORT:-30080}:9080"
      - "${APISIX_HTTPS_PUBLISHED_PORT:-30443}:9443"
      - "${APISIX_ADMIN_PUBLISHED_PORT:-9180}:9180"
    volumes:
      - ../gateway/config/config.dev.yaml:/usr/local/apisix/conf/config.yaml:ro
    depends_on:
      apisix-etcd:
        condition: service_healthy

  sso:
    build:
      context: ..
      dockerfile: apps/sso/Dockerfile
      args:
        <<: *build-proxy-args
        UMI_APP_API_PREFIX: ${UMI_APP_API_PREFIX:-/api/iam}
        UMI_APP_SSO_CLIENT_CODE: ${SSO_UMI_APP_SSO_CLIENT_CODE:-iam}
        UMI_APP_CAP_SITE_KEY: ${UMI_APP_CAP_SITE_KEY:-iam-sso}
        UMI_APP_CAP_ENDPOINT: ${UMI_APP_CAP_ENDPOINT:-/api/iam/open/cap/iam-sso/}
        UMI_APP_CAP_WASM_URL: ${UMI_APP_CAP_WASM_URL:-/portal/cap/cap_wasm_bg.wasm}
        UMI_APP_CAP_PAKO_URL: ${UMI_APP_CAP_PAKO_URL:-/portal/cap/pako_inflate.min.js}
        UMI_APP_LOGIN_CREDENTIAL_ALG: ${UMI_APP_LOGIN_CREDENTIAL_ALG:-SM2-SM4-CBC}
        UMI_APP_LOGIN_CREDENTIAL_KID: ${UMI_APP_LOGIN_CREDENTIAL_KID:-2026-05-primary}
        UMI_APP_LOGIN_CREDENTIAL_PUBLIC_KEY: ${UMI_APP_LOGIN_CREDENTIAL_PUBLIC_KEY:-04112ddd8854e8262db2520bba112535844884c03348a45fcf4ee0f9a967979be52bd46caf43be697ae557ed2e4fa5b4dca8d2dbfa08c0f2f710c0f61591bb17dc}
        UMI_APP_WELL_KNOWN_URL: ${UMI_APP_WELL_KNOWN_URL:-/sso/.well-known/authentication-configuration}
    restart: unless-stopped
    ports:
      - "${SSO_PUBLISHED_PORT:-30013}:80"

  admin:
    build:
      context: ..
      dockerfile: apps/admin/Dockerfile
      args:
        <<: *build-proxy-args
        UMI_APP_API_PREFIX: ${UMI_APP_API_PREFIX:-/api/iam}
        UMI_APP_SSO_AUTHORIZE_URL: ${UMI_APP_SSO_AUTHORIZE_URL:-/sso/authorize}
        UMI_APP_SSO_LOGOUT_URL: ${UMI_APP_SSO_LOGOUT_URL:-/sso/logout}
        UMI_APP_SSO_CLIENT_CODE: ${UMI_APP_SSO_CLIENT_CODE:-iam-admin}
        UMI_APP_ADMIN_ROLE_CODE: ${UMI_APP_ADMIN_ROLE_CODE:-iam:admin}
        UMI_APP_GRAFANA_URL: ${UMI_APP_GRAFANA_URL:-http://localhost:30030}
        UMI_APP_SYSTEM_LOG_ENV: ${UMI_APP_SYSTEM_LOG_ENV:-dev}
    restart: unless-stopped
    ports:
      - "${ADMIN_PUBLISHED_PORT:-30014}:80"

  api:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
      args: *build-proxy-args
    restart: unless-stopped
    labels:
      shgas-iam.logs.enabled: "true"
      shgas-iam.logs.env: dev
      shgas-iam.logs.service: api
      shgas-iam.logs.component: backend
    ports:
      - "${API_PUBLISHED_PORT:-30011}:30000"
    environment: *api-env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  admin-api:
    build:
      context: ..
      dockerfile: apps/admin-api/Dockerfile
      args: *build-proxy-args
    restart: unless-stopped
    labels:
      shgas-iam.logs.enabled: "true"
      shgas-iam.logs.env: dev
      shgas-iam.logs.service: admin-api
      shgas-iam.logs.component: backend
    ports:
      - "${ADMIN_API_PUBLISHED_PORT:-30012}:30001"
    environment: *admin-api-env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  oidc-provider:
    build:
      context: ..
      dockerfile: apps/oidc-provider/Dockerfile
      args: *build-proxy-args
    restart: unless-stopped
    labels:
      shgas-iam.logs.enabled: "true"
      shgas-iam.logs.env: dev
      shgas-iam.logs.service: oidc-provider
      shgas-iam.logs.component: backend
    ports:
      - "${OIDC_PUBLISHED_PORT:-30015}:30002"
    environment: *oidc-provider-env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  loki:
    image: ${LOKI_IMAGE:-docker.xuanyuan.run/grafana/loki:3.5.1}
    restart: unless-stopped
    profiles:
      - observability
    command:
      - -config.file=/etc/loki/loki.yaml
      - -config.expand-env=true
    ports:
      - "${LOKI_HTTP_PUBLISHED_PORT:-3100}:3100"
    volumes:
      - ../observability/loki/loki.dev.yaml:/etc/loki/loki.yaml:ro
      - loki_data:/loki

  grafana:
    image: ${GRAFANA_IMAGE:-docker.xuanyuan.run/grafana/grafana-oss:12.0.2}
    restart: unless-stopped
    profiles:
      - observability
    ports:
      - "${GRAFANA_HTTP_PUBLISHED_PORT:-30030}:3000"
    environment:
      GF_SERVER_ROOT_URL: ${GRAFANA_ROOT_URL:-http://localhost:30030}
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_AUTH_ANONYMOUS_ENABLED: ${GRAFANA_AUTH_ANONYMOUS_ENABLED:-true}
      GF_AUTH_ANONYMOUS_ORG_ROLE: ${GRAFANA_AUTH_ANONYMOUS_ORG_ROLE:-Admin}
      GF_AUTH_DISABLE_LOGIN_FORM: ${GRAFANA_AUTH_DISABLE_LOGIN_FORM:-false}
      GF_AUTH_GENERIC_OAUTH_ENABLED: ${GRAFANA_OIDC_ENABLED:-false}
      GF_AUTH_GENERIC_OAUTH_NAME: ${GRAFANA_OIDC_NAME:-IAM}
      GF_AUTH_GENERIC_OAUTH_ALLOW_SIGN_UP: ${GRAFANA_OIDC_ALLOW_SIGN_UP:-true}
      GF_AUTH_GENERIC_OAUTH_CLIENT_ID: ${GRAFANA_OIDC_CLIENT_ID:-grafana}
      GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET: ${GRAFANA_OIDC_CLIENT_SECRET:-}
      GF_AUTH_GENERIC_OAUTH_SCOPES: ${GRAFANA_OIDC_SCOPES:-openid profile}
      GF_AUTH_GENERIC_OAUTH_AUTH_URL: ${GRAFANA_OIDC_AUTH_URL:-http://localhost:30080/oidc/auth}
      GF_AUTH_GENERIC_OAUTH_TOKEN_URL: ${GRAFANA_OIDC_TOKEN_URL:-http://host.docker.internal:30080/oidc/token}
      GF_AUTH_GENERIC_OAUTH_API_URL: ${GRAFANA_OIDC_USERINFO_URL:-http://host.docker.internal:30080/oidc/me}
      GF_AUTH_GENERIC_OAUTH_USE_PKCE: ${GRAFANA_OIDC_USE_PKCE:-true}
      GF_AUTH_GENERIC_OAUTH_LOGIN_ATTRIBUTE_PATH: ${GRAFANA_OIDC_LOGIN_ATTRIBUTE_PATH:-preferred_username}
      GF_AUTH_GENERIC_OAUTH_NAME_ATTRIBUTE_PATH: ${GRAFANA_OIDC_NAME_ATTRIBUTE_PATH:-name}
      GF_AUTH_GENERIC_OAUTH_EMAIL_ATTRIBUTE_PATH: ${GRAFANA_OIDC_EMAIL_ATTRIBUTE_PATH:-preferred_username}
      GF_USERS_ALLOW_SIGN_UP: ${GRAFANA_USERS_ALLOW_SIGN_UP:-true}
      GF_USERS_AUTO_ASSIGN_ORG_ROLE: ${GRAFANA_AUTO_ASSIGN_ORG_ROLE:-Admin}
      GRAFANA_LOKI_URL: ${GRAFANA_LOKI_URL:-http://loki:3100}
      GRAFANA_DEV_ALERT_WEBHOOK_URL: ${GRAFANA_DEV_ALERT_WEBHOOK_URL:-http://127.0.0.1:9/iam-dev-null}
    extra_hosts:
      - host.docker.internal:host-gateway
    volumes:
      - grafana_data:/var/lib/grafana
      - ../observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ../observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - loki

  alloy:
    image: ${GRAFANA_ALLOY_IMAGE:-grafana/alloy:v1.9.2}
    restart: unless-stopped
    profiles:
      - observability
    command:
      - run
      - /etc/alloy/iam-logs.alloy
      - --server.http.listen-addr=0.0.0.0:12345
      - --storage.path=/var/lib/alloy/data
    ports:
      - "${ALLOY_HTTP_PUBLISHED_PORT:-12345}:12345"
    environment:
      ALLOY_LOG_LEVEL: ${ALLOY_LOG_LEVEL:-info}
      IAM_LOG_HOST: ${IAM_LOG_HOST:-dev-docker}
      LOKI_PUSH_URL: ${LOKI_PUSH_URL:-http://loki:3100/loki/api/v1/push}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - alloy_data:/var/lib/alloy/data
      - ../observability/alloy/iam-logs.dev.alloy:/etc/alloy/iam-logs.alloy:ro
    depends_on:
      - loki

volumes:
  db_data:
  redis_data:
  apisix_etcd_data:
  loki_data:
  grafana_data:
  alloy_data:
